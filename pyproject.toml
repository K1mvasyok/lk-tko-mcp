[project]
name = "lk-tko-mcp"
version = "0.1.0"
description = "MCP-сервер: инструментальный доступ Claude к системе ЛК (личный кабинет)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]",
    "python-dotenv>=1.2.3",
]
